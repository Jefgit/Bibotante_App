export type Candidates = {
    id: string, 
    position: string, 
    firstName: string, 
    lastName: string, 
    partyInitials: string
};

export type PartyList = {
    id: string,
    name: string
};